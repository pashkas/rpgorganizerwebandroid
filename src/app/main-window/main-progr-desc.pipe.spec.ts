import { MainProgrDescPipe } from './main-progr-desc.pipe';

describe('MainProgrDescPipe', () => {
  it('create an instance', () => {
    const pipe = new MainProgrDescPipe();
    expect(pipe).toBeTruthy();
  });
});
