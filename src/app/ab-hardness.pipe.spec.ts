import { AbHardnessPipe } from './ab-hardness.pipe';

describe('AbHardnessPipe', () => {
  it('create an instance', () => {
    const pipe = new AbHardnessPipe();
    expect(pipe).toBeTruthy();
  });
});
