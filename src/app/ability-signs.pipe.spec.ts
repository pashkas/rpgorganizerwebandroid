import { AbilitySignsPipe } from './ability-signs.pipe';

describe('AbilitySignsPipe', () => {
  it('create an instance', () => {
    const pipe = new AbilitySignsPipe();
    expect(pipe).toBeTruthy();
  });
});
