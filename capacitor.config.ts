import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'RPG Organizer',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
